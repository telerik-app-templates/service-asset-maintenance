global.notifications = {
    showErrorMessage: function (message) {
        // TODO: Style this.
        alert(message);
    },

    showNotification: function (message) {
        alert(message);
    }
}